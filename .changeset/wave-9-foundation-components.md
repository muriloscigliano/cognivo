---
'@cognivo/components': minor
'@cognivo/adapter-react': minor
'@cognivo/adapter-vue': minor
'@cognivo/gen-ui': minor
---

Add 5 new foundation components: `cg-avatar`, `cg-date-range-picker`, `cg-empty-state`, `cg-meter`, `cg-sidebar`.

Each ships with:

- Lit web component + Shadow DOM styling from design tokens
- ARIA semantics and keyboard support
- React and Vue wrappers with typed props
- Gen-UI library registration with Zod prop schemas
- Unit test coverage (render, props, events, a11y)

---
"@cognivo/gen-ui": minor
---

Register `ai-action-receipt` in the generative component library (props schema +
description) so agents can compose action receipts; restores registry parity
with @cognivo/components.

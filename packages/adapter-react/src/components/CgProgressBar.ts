import { createWrapper } from '../create-wrapper.js';
export const CgProgressBar = createWrapper('cg-progress-bar', ['value', 'label', 'showValue', 'variant', 'indeterminate', 'size', 'striped', 'animated'], {});

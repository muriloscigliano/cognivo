import { createWrapper } from '../create-wrapper.js';
export const CgCombobox = createWrapper('cg-combobox', ['options', 'value', 'multiple', 'placeholder', 'searchable', 'clearable', 'disabled', 'loading', 'name', 'open'], {});

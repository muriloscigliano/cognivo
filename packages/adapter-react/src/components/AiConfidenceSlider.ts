import { createWrapper } from '../create-wrapper.js';
export const AiConfidenceSlider = createWrapper('ai-confidence-slider', ['value', 'min', 'max', 'resultCount', 'totalCount', 'distribution'], {});

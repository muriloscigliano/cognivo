import { createWrapper } from '../create-wrapper.js';
export const AiTransformSlider = createWrapper('ai-transform-slider', ['rounded', 'beforeSrc', 'afterSrc', 'beforeLabel', 'afterLabel', 'position', 'orientation'], {});

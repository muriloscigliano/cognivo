import { createWrapper } from '../create-wrapper.js';
export const AiPersonalizationDash = createWrapper('ai-personalization-dash', ['rounded', 'preferences', 'segments', 'userName', 'lastUpdated', 'showReset'], {});

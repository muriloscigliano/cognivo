import { createWrapper } from '../create-wrapper.js';
export const AiSegmentationViewer = createWrapper('ai-segmentation-viewer', ['rounded', 'src', 'masks', 'selectedMask', 'opacity', 'showLabels', 'showLegend'], {});

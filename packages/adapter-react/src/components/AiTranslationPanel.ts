import { createWrapper } from '../create-wrapper.js';
export const AiTranslationPanel = createWrapper('ai-translation-panel', ['rounded', 'sourceText', 'targetText', 'sourceLang', 'targetLang', 'loading', 'confidence', 'alternatives'], {});

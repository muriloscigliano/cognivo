import { createWrapper } from '../create-wrapper.js';
export const AiRewardSignal = createWrapper('ai-reward-signal', ['rounded', 'score', 'maxScore', 'trend', 'history', 'label', 'description'], {});

import { createWrapper } from '../create-wrapper.js';
export const AiDetectionCanvas = createWrapper('ai-detection-canvas', ['rounded', 'src', 'detections', 'showLabels', 'showConfidence', 'selectedId', 'interactive'], {});
